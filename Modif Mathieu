#Dans la partie "Importation des librairies nécessaires"
import geodatasets
import re

# Dans la partie "Carte de genre"
Gender_card_by_way prend désormais en argument la filière que l'on veut afficher, donc mettre :
Gender_card_by_way(df,PASS)
Gender_card_by_way(df,BTS)
Gender_card_by_way(df,CPGE)


